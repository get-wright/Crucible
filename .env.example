# FPT AI Marketplace key. Two accepted formats:
#
#   1. this file containing ONLY the bare token on one line, or
#   2. an environment variable: FPT_API_KEY / MKP_API_KEY / CRUCIBLE_FPT_API_KEY
#
# Copy to `.env` and replace. Without a key Crucible still runs the whole
# pipeline against a deterministic scripted backend — see README §1.
sk-replace-me
